{
  "extends": "./tsconfig.json",
  // `exclude` overrides the base config's rather than merging with it, so
  // `public` has to be repeated here — see the note in tsconfig.json.
  "exclude": ["node_modules", "test", "dist", "public", "**/*spec.ts"]
}
